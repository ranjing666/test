# 单元 028 工作簿：DOM 与事件

- 最终产出：完成一个交互式计数器网页
- 配套单元讲义：`02_units/unit_028.md`
- 配套模板：`04_code_templates/unit_028_template.md`
- 配套答案：`05_code_solutions/unit_028_solution.md`
- 配套小测：`06_quizzes/unit_028_quiz.md`

## 本单元先做什么

1. 先看单元讲义，知道为什么学。
2. 再看模板，照着补一个最小版本。
3. 做下面的练习，不要求一步到位。
4. 最后再对照参考答案和小测。

## 本单元目标

- 知道 `选择页面元素` 在当前单元里负责什么。
- 知道 `点击事件` 在当前单元里负责什么。
- 知道 `输入事件` 在当前单元里负责什么。
- 知道 `动态修改页面` 在当前单元里负责什么。
- 最后做出：`完成一个交互式计数器网页`

## 热身问题

1. 你能不能用一句大白话解释 `选择页面元素`？
2. 你知不知道 `点击事件` 和前面内容的关系？
3. 你能不能说出最终产出 `完成一个交互式计数器网页` 的最小版本应该长什么样？

## 跟着做

1. 先把模板里的第一段内容手敲一遍，感受 `选择页面元素`。
2. 再补上第二块内容，感受 `点击事件`。
3. 再用你自己的话改一个小地方，把它和 `输入事件` 接起来。

## 基础练习

1. 做一个最小版本，只要能体现 `选择页面元素` 和 `点击事件` 就行。
2. 再补上 `输入事件`，让结果更完整。
3. 最后补上 `动态修改页面`，让它更接近 `完成一个交互式计数器网页`。

## 提升练习

1. 把命名改得更清楚。
2. 补一句英文说明，解释这个单元的产出在做什么。
3. 如果这是网页或接口，就让它多一块可见结果；如果这是算法或数据题，就多做一组输入输出。

## 英语练习

- 本单元词汇：`dom`, `event`, `click`, `input`, `update`, `element`
- 本单元句子：`I can update a page when the user clicks.`
- 先大声读 3 遍，再用中文讲出大意。
- 任选 3 个词，写 3 个很短的技术句子。

## 小项目推进建议

- 第一步：先完成 `完成一个交互式计数器网页` 的最小可运行版。
- 第二步：再让 `输入事件` 和 `动态修改页面` 进入成果里。
- 第三步：最后再整理命名、注释、说明文字。

## 自查清单

- [ ] 我知道 `选择页面元素` 是干什么的。
- [ ] 我知道 `点击事件` 和前面的关系。
- [ ] 我动手补过模板，而不是只看。
- [ ] 我已经开始做 `完成一个交互式计数器网页`。

## GitHub 提交建议

```bash
git add .
git commit -m "Finish unit 028 support pack practice"
git push
```

