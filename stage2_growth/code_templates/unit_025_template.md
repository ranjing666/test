# 单元 025 起步模板：CSS 布局与响应式

这一份不是最终答案，而是一个可以继续补、继续改的起步包。

## 文件：`index.html`

```html
<section class="card">
  <h1>CSS 布局与响应式</h1>
  <p>请继续补充页面内容。</p>
</section>
```

## 文件：`style.css`

```css
body {
  margin: 0;
  padding: 24px;
  background: #f4f6fb;
  font-family: "Microsoft YaHei", sans-serif;
}

.card {
  max-width: 560px;
  margin: 0 auto;
  padding: 24px;
  background: white;
  border-radius: 16px;
}
```

## 使用说明

1. 先手敲一遍，不要直接复制。
2. 跑起来或读一遍输出，再改一个小地方。
3. 卡住时回到单元讲义和工作簿。

## 建议提交

```bash
git add .
git commit -m "Work on unit 025 starter pack"
git push
```
