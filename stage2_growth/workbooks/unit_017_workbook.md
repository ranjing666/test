# 单元 017 工作簿：模块与 import

- 最终产出：把前面的工具函数拆成多个模块
- 配套单元讲义：`units/unit_017.md`
- 配套模板：`code_templates/unit_017_template.md`
- 配套答案：`code_solutions/unit_017_solution.md`
- 配套小测：`quizzes/unit_017_quiz.md`

## 本单元先做什么

1. 先看单元讲义，知道为什么学。
2. 再看模板，照着补一个最小版本。
3. 做下面的练习，不要求一步到位。
4. 最后再对照参考答案和小测。

## 本单元目标

- 知道 `import 标准库` 在当前单元里负责什么。
- 知道 `拆分自己的模块` 在当前单元里负责什么。
- 知道 `模块中的函数` 在当前单元里负责什么。
- 知道 `重复代码抽离` 在当前单元里负责什么。
- 最后做出：`把前面的工具函数拆成多个模块`

## 热身问题

1. 你能不能用一句大白话解释 `import 标准库`？
2. 你知不知道 `拆分自己的模块` 和前面内容的关系？
3. 你能不能说出最终产出 `把前面的工具函数拆成多个模块` 的最小版本应该长什么样？

## 跟着做

1. 先把模板里的第一段内容手敲一遍，感受 `import 标准库`。
2. 再补上第二块内容，感受 `拆分自己的模块`。
3. 再用你自己的话改一个小地方，把它和 `模块中的函数` 接起来。

## 基础练习

1. 做一个最小版本，只要能体现 `import 标准库` 和 `拆分自己的模块` 就行。
2. 再补上 `模块中的函数`，让结果更完整。
3. 最后补上 `重复代码抽离`，让它更接近 `把前面的工具函数拆成多个模块`。

## 提升练习

1. 把命名改得更清楚。
2. 补一句英文说明，解释这个单元的产出在做什么。
3. 如果这是网页或接口，就让它多一块可见结果；如果这是算法或数据题，就多做一组输入输出。

## 英语练习

- 本单元词汇：`module`, `import`, `library`, `reuse`, `organize`, `share`
- 本单元句子：`I can import code from another file.`
- 先大声读 3 遍，再用中文讲出大意。
- 任选 3 个词，写 3 个很短的技术句子。

## 小项目推进建议

- 第一步：先完成 `把前面的工具函数拆成多个模块` 的最小可运行版。
- 第二步：再让 `模块中的函数` 和 `重复代码抽离` 进入成果里。
- 第三步：最后再整理命名、注释、说明文字。

## 自查清单

- [ ] 我知道 `import 标准库` 是干什么的。
- [ ] 我知道 `拆分自己的模块` 和前面的关系。
- [ ] 我动手补过模板，而不是只看。
- [ ] 我已经开始做 `把前面的工具函数拆成多个模块`。

## GitHub 提交建议

```bash
git add .
git commit -m "Finish unit 017 support pack practice"
git push
```
