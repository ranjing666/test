# Day 349：API 与 JSON - 整合输出

- 所属阶段：第 3 阶段：高级进阶
- 所属单元：`unit_035`
- 单元最终产出：完成一个返回 JSON 的接口
- 建议时长：70 到 110 分钟

## 今天只要先抓这一件事

围绕单元产出拼成一个最小可运行成果。

## 今天先这样理解

这一单元的主线一直是 `完成一个返回 JSON 的接口`。今天不是追求学完全部，而是围绕当前焦点往前推进一小步，并且留下能回看的结果。

## 今日示例

```
import requests

response = requests.get("https://api.github.com")
print(response.status_code)
print(response.json())
```

## 今天要做什么

1. 今天把单元产出 `完成一个返回 JSON 的接口` 拼成最小完整版本。
2. 先保证主流程通，再慢慢补细节和美化。
3. 从头到尾演示一次，确认自己知道输入、处理、输出在哪里。

## 今天要打开哪些文件

- `stage3_advanced/units/unit_035.md`
- `stage3_advanced/workbooks/unit_035_workbook.md`
- `stage3_advanced/code_templates/unit_035_template.md`
- `stage3_advanced/code_solutions/unit_035_solution.md` 只在真的卡住时再看

## 今日英语

- 今日句子：`An API sends and receives JSON data.`
- 今天重点词：`response, data, endpoint`
- 先读 3 遍，再用中文说出大意。

## 常见错误

- 拿到 JSON 就慌，不先观察结构。
- 状态码和数据内容混为一谈。
- 接口出错时只看页面，不看错误信息。

## 今天最小产出

- 完成 `完成一个返回 JSON 的接口` 的最小可运行版本。

## 完成后检查

- [ ] 我今天至少亲手运行、验证或解释过 1 次。
- [ ] 我今天至少亲手改过 1 处。
- [ ] 我今天留下了一条真实的卡点记录。
- [ ] 我今天把零散知识拼成了一个更完整的成果。

## 复盘问题

- 我现在能不能把 `完成一个返回 JSON 的接口` 的最小版本从头讲给别人听？
- 这一单元最值得保留的代码、页面、结果或笔记是哪一个？
- 下一个单元开始前，我最该先补稳的薄弱点是什么？

## 学完后顺手做

- 去 `study_logs/day349.md` 写今天的记录；如果还没有，就先复制 `study_logs/daily_log_template.md`。
- 如果今天是这个单元的第 10 天，做完小测后执行一次 Git 提交。
