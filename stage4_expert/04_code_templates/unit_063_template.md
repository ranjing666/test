# 单元 063 起步模板：React 状态与事件

这一份不是最终答案，而是一个可以继续补、继续改的起步包。

## 文件：`package.json`

```json
{
  "name": "study-starter",
  "private": true,
  "scripts": {
    "dev": "vite"
  }
}
```

## 文件：`main.js`

```javascript
const message = "Please continue the task.";
console.log(message);
```

## 使用说明

1. 先手敲一遍，不要直接复制。
2. 跑起来或读一遍输出，再改一个小地方。
3. 卡住时回到单元讲义和工作簿。

## 建议提交

```bash
git add .
git commit -m "Work on unit 063 starter pack"
git push
```
