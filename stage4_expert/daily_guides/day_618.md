# Day 618：React 组件与 JSX - 核心主题 D / 挑战

- 所属阶段：第 4 阶段：专家深化
- 所属单元：`unit_062`
- 单元最终产出：完成一个由多个组件组成的页面
- 建议时长：70 到 110 分钟

## 今天只要先抓这一件事

尝试第四个核心点或小挑战，允许慢，但必须动手。

## 今天先这样理解

这一单元的主线一直是 `完成一个由多个组件组成的页面`。今天不是追求学完全部，而是围绕当前焦点往前推进一小步，并且留下能回看的结果。

## 今日示例

```
import { useState } from "react";

export default function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

## 今天要做什么

1. 围绕 `组件拆分` 做一个小挑战，先追求可运行。
2. 如果卡住，先缩小问题，不要直接放弃。
3. 卡到走不下去时，再对照 `stage4_expert/code_solutions/unit_062_solution.md`。

## 今天要打开哪些文件

- `stage4_expert/units/unit_062.md`
- `stage4_expert/workbooks/unit_062_workbook.md`
- `stage4_expert/code_templates/unit_062_template.md`
- `stage4_expert/code_solutions/unit_062_solution.md` 只在真的卡住时再看

## 今日英语

- 今日句子：`A component helps me build reusable UI.`
- 今天重点词：`props, render, ui`
- 先读 3 遍，再用中文说出大意。

## 常见错误

- 组件拆太多，但自己说不清数据从哪来。
- 状态更新了，却没理解为什么界面会重新渲染。
- 只会复制组件，不会先做最小可运行版本。

## 今天最小产出

- 完成一个围绕今天主题的最小练习。

## 完成后检查

- [ ] 我今天至少亲手运行、验证或解释过 1 次。
- [ ] 我今天至少亲手改过 1 处。
- [ ] 我今天留下了一条真实的卡点记录。

## 复盘问题

- 今天的主题 `React 组件与 JSX` 在真实项目里到底是干什么的？
- 我今天是看懂了一点，还是亲手跑通和改过了？
- 如果明天忘了，我今天的笔记和文件能不能把我拉回来？

## 学完后顺手做

- 去 `study_logs/day618.md` 写今天的记录。
- 如果今天是这个单元的第 10 天，做完小测后执行一次 Git 提交。
